"chat_settings"
{
    "vip_name_color"  // VIP feature key
    {
        /*
            Enter the color keys
            To access a custom variable, use: custom
            Enumeration through the ';'

            Max line length: 1024
        */

        "vip1"      "custom;{R};{G};{PI};{CO}"

        "vip2"      "custom;{R};{G};{PI};{T};{C};{B};{CO}"
    }

    "vip_message_color"
    {
        /*
            Enter the color keys
            To access a custom variable, use: custom
            Enumeration through the ';'

            Max line length: 1024
        */

        "vip1"      "custom;{R};{G};{PI};{CO}"

        "vip2"      "custom;{R};{G};{PI};{T};{C};{B};{CO}"
    }

    "vip_prefix"
    {
        /*
            Enter the translation keys of prefixes
            To access a custom variable, use: custom
            Enumeration through the ';'

            Max line length: 1024
        */

        "vip1"      "custom;p_lol;p_kek;p_cheb"

        "vip2"      "custom;p_lol;p_kek;p_cheb;p_alpha"
    }

    "vip_prefix_color"
    {
        /*
            Enter the color keys
            To access a custom variable, use: custom
            Enumeration through the ';'

            Max line length: 1024
        */

        "vip1"      "custom;{R};{G};{PI};{CO}"

        "vip2"      "custom;{R};{G};{PI};{T};{C};{B};{CO}"
    }
}